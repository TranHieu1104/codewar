#include <string>
#include <sstream>

using namespace std;

string sum_str(const string& a, const string& b) {
    stringstream sa(a), sb(b);
    int num_a = 0, num_b = 0;
    sa >> num_a;
    sb >> num_b;
    int sum = num_a + num_b;
    

}

